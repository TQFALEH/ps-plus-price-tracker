# SQLite database path
DATABASE_PATH=./data/ps-plus.db

# Server cache TTL in seconds (default 21600 = 6 hours)
CACHE_TTL_SECONDS=21600

# Max retry count when fetching source pages
FETCH_RETRY_COUNT=3
